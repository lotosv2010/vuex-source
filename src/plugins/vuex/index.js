import { Store, install } from './store'
import { mapState, mapGetters, mapMutations, mapActions } from './helper'

// 入口文件
export default {
  Store,
  install,
  mapState,
  mapGetters,
  mapMutations,
  mapActions
}

export {
  Store,
  install,
  mapState,
  mapGetters,
  mapMutations,
  mapActions
}